# Beat The Intro — Multiplayer (Supabase)

Host + Join:
- Host runs the game on the TV
- Players join on phones and buzz in

## Setup
1) Follow `SUPABASE_SETUP.txt`
2) Paste keys into `config.js`
3) Upload to GitHub Pages

## Audio clips
Add MP3 intros to `assets/tracks/` using filenames in `song_filenames.txt`.
